# Archived test scripts

These are early single-purpose probes that have been superseded by `../test-qa.js`. The QA runner has 14 tiers covering everything in here. Keep these only as historical reference — they don't run as part of any current workflow.

Do **not** ship to GitHub Actions / CI; point at `scripts/test-qa.js` instead.
